{
  "name": "geoffrey-infrastructure",
  "version": "0.0.0",
  "description": "",
  "repository": "",
  "private": true,
  "scripts": {
    "preinstall": "cd geoffrey && npm install; cd ..; cd promo && npm install && node_modules/.bin/grunt build"
  }
}
